server {
    listen 80;
    server_name dadamda.duckdns.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'DaDamDa: waiting for TLS certificate bootstrap.';
        add_header Content-Type text/plain;
    }
}
